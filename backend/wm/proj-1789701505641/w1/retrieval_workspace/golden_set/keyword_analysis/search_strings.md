# Search Strings

## Primary Search String (High Precision)
```
("graph neural") AND ("neural networks") AND ("graph")
```

## Secondary Search String (Higher Recall)
```
("graph" OR "neural" OR "networks" OR "including" OR "gaps" OR "increasingly" OR "covering" OR "graph neural" OR "neural networks" OR "subgraph" OR "interpretability" OR "identify" OR "mask" OR "search" OR "gradient") AND ("general")
```

## Database-Specific Variants

### ACM Digital Library
```
[All: ("graph neural") AND ("neural networks") AND ("graph")]
```

### IEEE Xplore
```
("graph neural") AND ("neural networks") AND ("graph")
```

### Springer
```
("graph neural") AND ("neural networks") AND ("graph")
```

### Elsevier / ScienceDirect
```
("graph neural") AND ("neural networks") AND ("graph")
```

### arXiv
```
all:graph neural AND all:neural networks AND all:graph
```

### Semantic Scholar
```
graph neural neural networks graph neural
```

### Google Scholar
```
("graph neural") AND ("neural networks") AND ("graph")
```
